media:
  folder: public/media

collections:
  - name: poems
    label: Poems
    folder: src/pages/poems
    extension: md
    format: frontmatter
    fields:
      - name: title
        label: Title
        type: string
        required: true
      - name: layout
        label: Layout Path
        type: string
        default: ../../layouts/BaseLayout.astro
        hidden: true # Hides this from the dashboard so you don't accidentally change it
      - name: body
        label: Poem Content
        type: rich-text